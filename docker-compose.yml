version: '3.8'

services:
  todo-app:
    build: 
      context: .
      dockerfile: Dockerfile
      # Alternative: Use secure Dockerfile with build args
      # dockerfile: Dockerfile.secure
      # args:
      #   MONGODB_URI: mongodb+srv://wisakov432:wgueq3w4rlIzLQ3U@cluster0.pustd.mongodb.net/todo-app?retryWrites=true&w=majority
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - MONGODB_URI=mongodb+srv://wisakov432:wgueq3w4rlIzLQ3U@cluster0.pustd.mongodb.net/todo-app?retryWrites=true&w=majority
    restart: unless-stopped
    networks:
      - todo-network

networks:
  todo-network:
    driver: bridge